#include<stdio.h>
#include<conio.h>

int main()
{
   int a[10];
   int i;
   for(i=0;i<10;i++)
   {
      scanf("%d",&a[i]);
   }
   sum = SumOfArray(a);
   printf("%d",sum);
}

int SumOfArray(int a[])
{
   int i,sum=0;
   for(i=0;i<10;i++)
   {
      sum = sum + a[i];
   }
   return sum;
 }
